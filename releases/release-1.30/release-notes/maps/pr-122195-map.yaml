pr: 122195
releasenote:
  text: "kube-scheduler implements scheduling hints for the `PodTopologySpread` plugin.\nThe
    scheduling hints allow the scheduler to retry scheduling a Pod\nthat was previously
    rejected by the `PodTopologySpread` plugin if \ncreate/delete/update a related
    Pod or a node which matches the toplogyKey."
